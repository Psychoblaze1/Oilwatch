# Copy to `.env` and paste your real key. Never commit the real file.
ANTHROPIC_API_KEY=sk-ant-replace-me

# Model for the AI co-pilot. Defaults to the most capable model.
ANTHROPIC_MODEL=claude-opus-4-8

# Let the AI pull live news / prices via web search (on | off).
WEB_SEARCH=on

PORT=3000
