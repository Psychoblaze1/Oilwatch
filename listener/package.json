{
  "name": "lab88-listener",
  "version": "0.1.0",
  "description": "Lab88 instrument listener — watches files / serial / TCP and uploads parsed readings to the Lab88 server as draft samples.",
  "main": "main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "dev": "electron . --enable-logging"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "chokidar": "^3.6.0"
  },
  "optionalDependencies": {
    "serialport": "^12.0.0"
  },
  "devDependencies": {
    "electron": "^31.0.0"
  }
}
